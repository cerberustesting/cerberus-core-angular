import { UniqueproplistPipe } from './uniqueproplist.pipe';

describe('UniqueproplistPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueproplistPipe();
    expect(pipe).toBeTruthy();
  });
});
