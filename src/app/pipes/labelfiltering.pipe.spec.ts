import { LabelfilteringPipe } from './labelfiltering.pipe';

describe('LabelfilteringPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelfilteringPipe();
    expect(pipe).toBeTruthy();
  });
});
